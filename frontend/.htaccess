Options -Indexes
RewriteEngine On

# Redirect old voice-agent.html to folder URL
RewriteRule ^voice-agent\.html$ /voice-agent/ [R=301,L]

# Standard fallback
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ /index.html [L]
